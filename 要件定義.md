# コードメモアプリ

## 概要

鳩屋敷がコード進行のアナライズをしたものをメモしたり保存できたりするWebアプリ

## 要件

- ブラウザで操作できる
- 曲のタイトルを設定できる
- スケールを設定できる(メジャースケールのみでひとまずOK)
  - 転調にも対応
- アナライズ結果を再生できる
  - 音声は各音階の単音データを持っておき、再生前に鳴らす音声を事前計算しておき、BPMに合わせて拍頭で鳴らす
  - ルート音は守りつつ、その上のオクターブ内で、なるべく運指に無理がないような転回系で鳴らす
- BPMを設定できる
- Youtube、ニコニコ動画のURLをオプショナルで設定できる
- ディグリーは1拍〜4拍の単位で指定可能
- アナライズで使用できるディグリーは以下の組み合わせ
  - Ⅰ〜Ⅶ、Ⅰ♯、Ⅱ♯、Ⅳ♯、Ⅴ♯、Ⅵ♯
    - ♭Ⅲ、♭Ⅵ、♭Ⅶは、マイナーやdimなどのついていないこの形のみフラット表記
  - m(マイナー)
  - ドミナントセブンス
  - dim、aug
  - 転回系、オンコード
- デプロイ先はCloudFlare、データベースはD1、IaCは必須
- スマホでの編集・閲覧も可能
- 閲覧は誰でも可能、編集は私のみ可能
- この範囲は〇〇進行、みたいなデータを持たせたい
- 曲ごとにタグをつけたい

## MVP確定仕様

### 公開・所有権

- トップページは公開日時の新しい順に12件ずつページネーションする
- 曲メモは下書きまたは公開済みの状態を持つ
- 公開日時は初回公開時に記録し、更新や再公開では変更しない
- Google OAuthでログインする
- 編集を許可するメールアドレスはCloudflareの `ALLOWED_EMAILS` で管理する
- 曲メモには `created_by_user_id`（表示上は「作成者」）を持たせる
- 下書きの閲覧、公開・非公開化、編集、削除は作成者本人だけが行える
- 管理者ロールはこのアプリには設けない

### 拍と編集

- MVPの拍子は4/4固定とする
- コードまたは `N.C.` は1〜4拍の区間を占める
- コードは小節線をまたがず、小節境界で同じコードのブロックへ自動分割する
- 未入力・無音の時間はデータ上 `N.C.` とし、末尾に連続する `N.C.` は閲覧時に隠す
- 編集中は適用幅（1〜4拍）を保持し、タイムラインの開始位置を選ぶとその幅へ一括適用する
- 既存ブロックの一部を上書きした場合は前後を自動分割し、小節内で隣接する同一コードは結合する
- 入力変更は800msデバウンスし、自動保存の状態を表示する

### コード

- 内部のディグリーは主音からの半音値0〜11で統一する
- UIでは接尾辞なしの `Ⅱ♯・Ⅴ♯・Ⅵ♯` を `♭Ⅲ・♭Ⅵ・♭Ⅶ` と表示する
- それ以外の非ダイアトニックコードとオンコードのベースはシャープ表記にする
- コード種別はメジャー、マイナー、ドミナントセブンス、dim、aug、ø7とする
- ベース音はディグリーで指定する。コード本体と同じベース指定は保存・表示しない
- 初期パレット:
  - ダイアトニック: `Ⅰ / Ⅱm / Ⅲm / Ⅳ / Ⅴ / Ⅵm / Ⅶdim`
  - 同主短調借用: `Ⅰm / ♭Ⅲ / Ⅳm / Ⅴm / ♭Ⅵ / ♭Ⅶ`
  - ドミナントセブンス: `Ⅰ7 / Ⅱ7 / Ⅲ7 / Ⅳ7 / Ⅴ7 / Ⅵ7 / Ⅶ7`
  - 上記に加え、全ディグリー、コード種別、ベース音の自由選択を設ける

### キーと転調

- キーと転調は各コードへ重複保持せず、曲の初期キーと開始拍を持つ転調イベントとして保存する
- UIのキー名は `C, D♭, D, E♭, E, F, F♯, G, A♭, A, B♭, B` とする

### タグと進行範囲

- タグと進行名は共有候補として再利用でき、許可ユーザーは新しい名称も作成できる
- 進行範囲はコードブロック単位の開始・終了位置を持つ
- 同じコードブロックが複数の進行範囲へ含まれることを許可する

### 再生

- コード開始時に1回鳴らし、次のコード開始時に停止する
- WAVがコード長より短い場合はループせず、自然な減衰に任せる
- ベース未指定時はルートを最低音とし、低音をMIDIノート36〜48へ置く
- ベース指定時は指定音を最低音にし、構成音をその上へ配置する
- 転調イベントに基づき再生前に各コードのMIDIノートを計算する

### 配色

- コード以外のUIは無彩色を中心に、プライマリ、リンク、デンジャーの操作色だけを使う
- コード色はSoundQuestベースのT/P/Pm/S/Sm/D/Dm分類を使う
- 重複は単色へ解決し、`♭Ⅶ/Ⅰ → Dm`、`♭Ⅶ → Dm`、`Ⅰaug → T`、`♯Ⅳm → P`、`Ⅲø7 → T` とする

### MVP対象外

- 3/4や変拍子
- YouTube・ニコニコ動画の埋め込みと同期再生
- タグ・進行名マスターの管理画面
- アプリ内の管理者ロール
